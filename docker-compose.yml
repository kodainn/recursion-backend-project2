version: '3'
services:
  python_ubuntu:
    image: python_ubuntu
    volumes:
      - type: bind
        source: .
        target: /python_practice
    stdin_open: true
    tty: true
    command: bash
